{
  "name": "marquee",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "app": "tauri dev",
    "tokens": "python3 tools/build-tokens.py",
    "logs": "tail -f \"$HOME/Library/Logs/Marquee/marquee.log\"",
    "test": "vitest run && tsc --noEmit && cd src-tauri && cargo test",
    "test:ui": "vitest run"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.2"
  }
}
